@model ChapterFormViewModel

@{
    ViewData["Title"] = "Tạo chương";
}

<div class="row justify-content-center">
    <div class="col-lg-7">
        <div class="mb-4">
            <a asp-action="Details" asp-route-id="@Model.SubjectId" class="text-decoration-none">&larr; Quay lại môn học</a>
            <h1 class="display-6 mt-2">Tạo chương</h1>
            <p class="text-muted mb-0">Môn học: @Model.SubjectName</p>
        </div>

        <div class="card border-0 shadow-sm">
            <div class="card-body p-4">
                <form asp-action="CreateChapter" method="post">
                    <input asp-for="SubjectId" type="hidden" />
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                    <partial name="_ChapterForm" model="Model" />

                    <div class="d-flex justify-content-end gap-2">
                        <a asp-action="Details" asp-route-id="@Model.SubjectId" class="btn btn-outline-secondary">Hủy</a>
                        <button type="submit" class="btn btn-primary">Tạo chương</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
